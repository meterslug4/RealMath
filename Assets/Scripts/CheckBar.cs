using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CheckBar : MonoBehaviour
{
    //public GameObject parent;
    //private void OnTriggerEnter(Collider other)
    //{
    //    if(other.CompareTag("Edge")==true)
    //    {
    //            if (parent.GetComponent<DesEdgeRoot>().vertexsPoint.Contains(other.transform.position))
    //            {
    //                Debug.Log("포함안함");
    //            }
    //            else
    //            {
    //                parent.GetComponent<DesEdgeRoot>().vertexsPoint.Add(other.transform.position);
    //            }
    //    }
    //}
}
